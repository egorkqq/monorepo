import { atom } from "jotai";

export const sendingAssetAtom = atom<string | null>(null);
export const sendingAmountAtom = atom<string | null>(null);
export const sendingAddressAtom = atom<string | null>(null);
